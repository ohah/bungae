# Cursor ignore file
# 이 파일은 Cursor 에디터에서 파일 탐색 시 제외할 파일/폴더를 지정합니다.
# dist 폴더는 빌드 결과물이므로 에디터에서 확인할 수 있도록 제외하지 않습니다.

# node_modules는 제외
node_modules/

# .git은 제외
.git/

# OS 파일은 제외
.DS_Store
Thumbs.db

# Log 파일은 제외
*.log

# .gitignore에 있지만 에디터에서 보이게 하고 싶은 폴더는 여기서 제외하지 않음
# dist/ - 제외하지 않음 (에디터에서 보이게 함)
# build/ - 제외하지 않음 (에디터에서 보이게 함)
